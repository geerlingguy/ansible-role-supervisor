---
- name: Copy Supervisor init file into place (for non-systemd systems).
  template:
    src: "supervisor.init-{{ ansible_os_family }}.j2"
    dest: /etc/init.d/supervisor
    owner: root
    group: root
    mode: 0755
  when: "ansible_service_mgr != 'systemd'"
  notify: restart supervisor

- name: Copy Supervisor unit file into place (for systemd systems).
  template:
    src: supervisor.unit.j2
    dest: /etc/systemd/system/supervisor.service
    owner: root
    group: root
    mode: 0755
  when: "ansible_service_mgr == 'systemd'"
  notify: restart supervisor
